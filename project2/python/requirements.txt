# Minimal Python runtime used by the BBB deployment path.
# Install with:
#   python3 -m pip install -r requirements.txt
#
# Notes:
# - The protobuf module in proto/log_message_pb2.py is a lightweight local stub,
#   so the google protobuf package is not required at runtime.
# - BBB systems should prefer Debian packages first:
#     sudo apt-get install -y python3-flask python3-zmq
Flask>=2.2,<2.3
pyzmq>=22,<25
